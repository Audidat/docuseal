# Production Environment Configuration
# Copy this file to .env.prod and fill in your values
# DO NOT commit .env.prod to git!

# =============================================================================
# APPLICATION
# =============================================================================
APP_HOST=example.com
FORCE_SSL=true

# Email for Let's Encrypt SSL certificates
ACME_EMAIL=admin@example.com

# =============================================================================
# SECRETS (GENERATE UNIQUE VALUES!)
# =============================================================================
# Generate with: openssl rand -hex 64
SECRET_KEY_BASE=your-secret-key-base-here-generate-with-openssl-rand-hex-64

# Generate with: openssl rand -hex 32
ENCRYPTION_KEY=your-encryption-key-here-generate-with-openssl-rand-hex-32

# =============================================================================
# DATABASE
# =============================================================================
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your-strong-postgres-password-here
POSTGRES_DB=docuseal_production

# =============================================================================
# REDIS
# =============================================================================
REDIS_PASSWORD=your-strong-redis-password-here

# =============================================================================
# DIGITAL SIGNATURE (DSS Service)
# =============================================================================
# Path to your PKCS12 certificate file (must exist on host)
P12_CERT_PATH=/path/to/your/certificate.p12

# Password for the P12 certificate
P12_PASSWORD=your-p12-password-here

# Timestamp Authority URL (default: DigiCert)
TSA_URL=http://timestamp.digicert.com

# =============================================================================
# EMAIL (SMTP) - Optional but recommended
# =============================================================================
# Leave empty to disable email sending
SMTP_ADDRESS=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_DOMAIN=gmail.com
SMTP_FROM=noreply@example.com

# =============================================================================
# STORAGE - Optional (defaults to local filesystem)
# =============================================================================
# Options: local, amazon, google, microsoft
ACTIVE_STORAGE_SERVICE=local

# AWS S3 (if using amazon)
S3_BUCKET=your-bucket-name
S3_REGION=us-east-1
S3_ACCESS_KEY_ID=your-access-key
S3_SECRET_ACCESS_KEY=your-secret-key

# Google Cloud Storage (if using google)
# GCS_BUCKET=your-bucket-name
# GCS_PROJECT=your-project-id
# GCS_KEYFILE=/path/to/keyfile.json

# Azure Blob Storage (if using microsoft)
# AZURE_STORAGE_ACCOUNT=your-account-name
# AZURE_STORAGE_ACCESS_KEY=your-access-key
# AZURE_STORAGE_CONTAINER=your-container-name

# =============================================================================
# PERFORMANCE
# =============================================================================
# Number of web server processes
WEB_CONCURRENCY=2

# Number of threads per process
RAILS_MAX_THREADS=5

# =============================================================================
# PORTS (Optional - defaults shown)
# =============================================================================
HTTP_PORT=80
HTTPS_PORT=443

# =============================================================================
# MONITORING (Optional)
# =============================================================================
# Sentry DSN for error tracking
# SENTRY_DSN=https://your-sentry-dsn

# New Relic license key
# NEW_RELIC_LICENSE_KEY=your-key

# =============================================================================
# NOTES
# =============================================================================
# 1. Generate SECRET_KEY_BASE: openssl rand -hex 64
# 2. Generate ENCRYPTION_KEY: openssl rand -hex 32
# 3. Generate POSTGRES_PASSWORD: openssl rand -base64 32
# 4. Generate REDIS_PASSWORD: openssl rand -base64 32
# 5. Set APP_HOST to your actual domain name
# 6. Set ACME_EMAIL to receive SSL certificate expiration notices
# 7. Place your P12 certificate file and update P12_CERT_PATH
